# Rebuilds the community list from what the intake accepted, and OPENS A PULL
# REQUEST rather than committing.
#
# The pull request is the whole point of the design, not caution for its own
# sake. Anybody on the internet can POST a record to the intake; a poisoned row
# that published itself would be downloaded by every Tandem on earth, and the
# client treats the list as a suggestion it asks about - so one bad row reaching
# one machine is a question, while one bad row reaching the list is a question
# asked of everybody. Those are different sizes of mistake.
#
# It needs no secret. tools/monta-lista.py reads a public URL, which is why
# api/acumulado.js exists: listing the store directly would need the read-write
# token in this repository's secrets, and the whole arrangement was chosen to
# avoid having a secret anybody had to create by hand.
name: lista

on:
  schedule:
    # Weekly. The list is a filter list, not a feed: a shop that learns
    # something today is not waiting for the world to know by tonight.
    - cron: "17 5 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  montar:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7

      - name: Rebuild from the accepted records
        run: |
          python3 tools/monta-lista.py \
            "https://tandem-psi-ten.vercel.app/api/acumulado" | tee saida.txt

      # Signing. The ONE hand-made secret in this project, and it is here
      # because the owner decided the trade-off knowingly: a key in Actions
      # means whoever controls this repository can sign, which is most of the
      # threat back again - but it costs him nothing weekly, and the realistic
      # attack (a modified copy served from somewhere that is not GitHub) is
      # closed by it. CLAUDE.md said "no secret is created by hand anywhere in
      # the chain" for four versions; that sentence is now wrong, and the note
      # beside it says so rather than being quietly deleted.
      #
      # It signs whatever the rebuild produced, in the same job, before the
      # pull request - so what a human reviews on the PR page is exactly the
      # bytes that were signed.
      - name: Sign the rebuilt list
        env:
          LISTA_CHAVE_PRIVADA: ${{ secrets.LISTA_CHAVE_PRIVADA }}
        run: |
          set -euo pipefail
          # The absence check is INSIDE the script on purpose. A step-level
          # `if:` reading its own `env:` is the kind of expression that either
          # always skips or always runs depending on how the runner resolves
          # contexts, and both failure modes are silent: one never signs and
          # says nothing, the other fails every rebuild. Here it is a plain
          # shell test whose behaviour is obvious from reading it.
          if [ -z "${LISTA_CHAVE_PRIVADA:-}" ]; then
            echo "no signing key configured yet - publishing the list unsigned"
            exit 0
          fi
          if [ ! -f src/lib/lista-publica.pem ]; then
            echo "a private key is configured but no public key is committed;"
            echo "signing now would produce a signature nothing can check."
            exit 1
          fi
          umask 077
          printf '%s' "$LISTA_CHAVE_PRIVADA" > /tmp/lista.key
          # -rawin is what Ed25519 needs: it signs the message, not a digest.
          openssl pkeyutl -sign -inkey /tmp/lista.key -rawin \
              -in lista/lista.tsv -out /tmp/lista.sig
          base64 -w0 < /tmp/lista.sig > lista/lista.tsv.sig
          shred -u /tmp/lista.key /tmp/lista.sig 2>/dev/null || rm -f /tmp/lista.key /tmp/lista.sig
          # Proves the signature verifies against the PUBLISHED public key
          # before it goes anywhere near a pull request. A signature nobody
          # checked is a signature that can be wrong for a month.
          base64 -d < lista/lista.tsv.sig > /tmp/check.sig
          openssl pkeyutl -verify -pubin -inkey src/lib/lista-publica.pem -rawin \
              -in lista/lista.tsv -sigfile /tmp/check.sig
          rm -f /tmp/check.sig
          echo "signature verifies against the shipped public key"

      - name: Open a pull request when the list actually changed
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if git diff --quiet -- lista/lista.tsv; then
            echo "nothing changed; no pull request"
            exit 0
          fi
          # A branch per run, so two runs cannot fight over one ref.
          ramo="lista/$(date -u +%Y-%m-%d-%H%M)"
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b "$ramo"
          git add lista/lista.tsv
          git commit -m "The community list, rebuilt from the accepted records

          $(cat saida.txt)"
          git push -u origin "$ramo"
          # The body carries what was left out as well as what went in. A row
          # that vanishes silently between the intake and the published file is
          # how a list stops describing what people actually reported.
          gh pr create \
            --title "The community list, rebuilt from the accepted records" \
            --body "$(printf 'Rebuilt by .github/workflows/lista.yml from what the intake accepted.\n\n```\n%s\n```\n\nRead the rows before merging: anybody can POST to the intake, and this is the gate that stops a poisoned row from reaching every Tandem at once.\n' "$(cat saida.txt)")" \
            --base main --head "$ramo"
